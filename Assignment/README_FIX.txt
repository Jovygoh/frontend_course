STRICT FIX FINAL v2

Only two corrections were made:
1. Fixed JavaScript initialization order so Complete Registration and all page-specific
   handlers initialize after their data/constants/functions are defined.
2. Corrected the initial theme to dark so pages do not unexpectedly start white.
   The existing Mode toggle is still retained.

No page layout, wording, cards, forms, navigation, or CSS design was otherwise changed.
